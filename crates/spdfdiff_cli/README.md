# spdfdiff_cli

CLI entry point for `spdfdiff diff`, `inspect`, `extract`, and `corpus`.

The current `diff` vertical slice extracts text from the first page and consumes
single-stream or ordered multi-stream page content.
