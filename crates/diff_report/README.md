# diff_report

Stable JSON and Markdown report generation. The CLI currently wraps Markdown in
basic self-contained HTML; richer HTML/SVG report generation remains deferred.

Markdown change lists include available old/new node evidence text and page
references so generated HTML carries the same semantic content.
