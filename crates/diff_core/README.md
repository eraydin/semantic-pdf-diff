# diff_core

Semantic block matching, text hunks, move detection, confidence, and severity defaults.

Current matching uses deterministic exact normalized-text anchors before
emitting inserted, deleted, or modified paragraph changes. Fuzzy matching, move
detection, text hunks, and layout-only changes remain later MVP work.
